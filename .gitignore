# dependencies / build
node_modules/
.next/
out/

# runtime data store (events, briefs, order drafts) — regenerated at runtime
data/

# env — never commit keys
.env
.env.local
.env.*.local

# misc
*.tsbuildinfo
next-env.d.ts
.DS_Store
.mimosa/
logs/
